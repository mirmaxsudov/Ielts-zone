spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ielts
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: IeltsZoneFileService

server:
  port: 8083

logging:
  level:
    org:
      hibernate: INFO
      springframework: INFO